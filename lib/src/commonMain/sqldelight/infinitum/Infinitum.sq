CREATE TABLE infinitum (
	name TEXT NOT NULL,
	number INTEGER NOT NULL
);

CREATE INDEX infinitum_number ON infinitum(number);

INSERT INTO infinitum (number, name)
VALUES (10, 'Tiaguito');

selectAll:
SELECT * 
FROM infinitum;

insert:
INSERT INTO infinitum(number, name)
VALUES (?, ?);
